## 所有项目推送 GitHub 前的隐私检查

每次用户准备把任何项目 push 到 GitHub 前，必须先做隐私检查，并先把检查结果告诉用户，等待用户确认后再 push。

检查范围包括但不限于：
- API keys
- Tokens
- Secrets
- Cookies
- 私钥
- 管理员账号密码
- 数据库账号密码
- 第三方服务配置
- 本地数据库文件
- storage 数据文件
- 日志文件
- 缓存文件
- 上传文件
- 测试账号密码

要求：
- 不要直接 push。
- 如果发现敏感配置写死在代码里，改成从 `.env` 读取。
- 创建或更新 `.env.example`，只保留示例值，不放真实密钥。
- 确认 `.env`、本地数据库、storage、日志、缓存等不会被 Git 提交。
- 检查 `git status` 和已跟踪文件，确认没有敏感文件会被提交。
- 如果发现敏感文件已经被 Git 跟踪，先告诉用户，不要擅自删除历史记录。
- 如果不确定某个文件能不能公开，先问用户，不要默认提交。
